# 阶段文档整理审计（2026-07-22）

## 目标

使 `proj-info/phases/`、根 `README.md` 与 `AGENTS.md` 同步反映：P1/P2 已完成、
normal-vNext 是唯一产品机器人、P2.5 为可恢复但不可执行的历史归档，以及 P3 是下一
可启动阶段。

## 执行步骤

1. [完成] 盘点 `phases/` 的目录、文档职责及 P2.5 撤销后的引用冲突。
2. [完成] 更新阶段总表、阶段入口与根文档的状态和链接。
3. [完成] 为 P2.5 历史目录建立统一归档说明，不移动历史文件。
4. [完成] 核对 Markdown 链接和 Git 差异，提交文档整理。

## 验证

- 已检查 `proj-info/phases/` 全部现存目录及 Markdown 文件。
- 已消除 P2.5 工作树中指向已撤销专家牌例目录的活动链接。
- `git diff --check` 通过；本次仅修改 Markdown 控制与说明文件，无生产代码改动。

## 决策

- 不移动 P1、P2、P2.5 的现有文件：Git 历史已经可恢复，物理迁移会破坏已知链接。
- `P2.5/` 保留为历史归档目录；`tasks.md` 和目录 README 是唯一运行状态入口。
- 新的策略工作记录写入 `proj-info/research/` 或未来的正常阶段任务，不再创建 P2.5
  编号任务。
