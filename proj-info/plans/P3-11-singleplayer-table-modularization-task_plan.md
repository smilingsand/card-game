# P3-11 单人牌桌模块化（第一步）

## 目标

仅审计并在确有必要时重构单人 `frontend/src/App.tsx`，为后续多人版复用建立无状态展示与交互边界；本任务不修改多人前端或后端。

## 范围与硬约束

- 不修改 `frontend/src/multiplayer/`、`backend/` 或规则/存档格式。
- 不改变规则、策略、固定 seed 回放或单人 UI 行为。
- 共享层不得取得完整会话、隐藏手牌、全量牌表、seed、IndexedDB、网络或 Authority 状态。
- 完成第一步后暂停，不进入多人整合。

## 阶段

1. **complete** — 审计 `App.tsx`、依赖与现有测试，逐项定位 14 项职责。
2. **complete** — 确认需要最小拆分，并定义可复用 props 边界。
3. **complete** — 增加共享操作区行为回归，并进行仅限单人侧的最小重构。
4. **complete** — 已完成格式、类型、lint、前端完整测试和构建验证；记录调用链、文件职责和本地 Preview 方法。
5. **complete** — 已交付第一步结论；等待用户新指令，不进入第二步。
6. **complete** — 已固化共享牌桌输入合同，并由单人适配器生成该合同。
7. **complete** — 已为合同、投影清理、按钮独立状态、公开信息边界和座位映射补独立测试。
8. **complete** — 自动验证和隔离浏览器单人 smoke test 已通过；已以 `P4 - ` 前缀提交当前分支工作。

## 决策记录

- 2026-07-27：先审计，禁止以修复多人症状为由修改多人代码。
- 2026-07-27：用户授权在当前单人模块化分支完成类型与测试收口并提交；仍禁止开始多人整合。

## 错误

- 2026-07-27：一次 PowerShell 审计命令因双引号转义错误未执行；改用单引号包裹 `rg` 模式，未修改任何产品文件。
- 2026-07-27：首次读取 TDD 技能时误用了不存在的根路径；将改读技能目录清单中给出的绝对路径，未修改任何产品文件。
- 2026-07-27：一次全量 App 测试中的单个用例超时；受控定向重跑通过，未发现产品行为失败。随后普通权限下的 Vite 配置加载报 `.vite-temp` EPERM；按交接约定使用受控提升权限重跑通过，未删除依赖目录。
- 2026-07-27：普通权限下 `npm.cmd run build` 同样因 `.vite-temp` EPERM 失败；受控提升权限重跑构建通过。
- 2026-07-27：在 `codex/p3-11-singleplayer-table-modularization` 分支重跑全前端测试时，`MultiplayerApp.test.tsx` 的“重新连接”断言失败（`client.connect` 调用 2 次而非预期的 1 次）；本次未修改多人代码。仅单人 `src/App.test.tsx` 在受控运行中 21/21 通过。
