# P7 发布记录

尚未发布。

P7-00 仅建立策略治理和离线基线运行器，不改变 normal-vNext 决策、规则、UI、存档或网络协议，
因此不需要 Preview 或 Production 验收。任何 P7 策略行为变更均须在 P7-05 完成新的
Preview 与 Production 验收后才可发布。

## 2026-08-26：P7-05 本地收敛证据

- 固定 seed `0, 1, 7, 42, 99` 的 normal-vNext 首次决策基准通过；每个动作均来自规则层合法动作集，最慢单次决策低于 5 秒。
- 此记录不是发布：未创建 Preview、未合并 `main`、未执行 Production 验收，线上仍维持原已发布版本。
- 回滚目标：若后续 Preview 或 Production 验证发现策略回归，回退 P7 系列提交至 `3507d7c` 的 P7-00 基线策略版本。

## 2026-08-27：P7-12 跟牌性能本地收敛

- 对大量红桃级牌配牌投影，规则层仍返回完整合法动作集；normal-vNext 仅将固定最多 24 个低资源候选送入深度路线评分，避免浏览器主线程按投影数量重复计算。
- P7-12 固定牌例、类型、lint、格式与 5 seed 专项基准均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-13 尾局领牌炸弹保护补齐

- 谨慎尾局领牌（下家剩 4–5 张）不再绕过天然完整炸弹保护；下家 1–3 张的公开强制阻断、直接收尾与显著路线收益仍保留为例外。
- P7-13 固定牌例、类型、lint、格式与 5 seed 专项基准均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-14 三带二争牌意愿修正

- K 不再与 A、级牌、大小王同列为最高控制资源；完整自然 `KKK44` 可在中等三带二上主动压制，同时继续保留炸弹和真正高控制资源。
- P7-14 固定牌例、类型、lint、格式与 5 seed 专项基准均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-15 全牌型普通跟牌意愿提升

- 对手持权时，完整、非控制且不造成结构损伤的普通响应获得统一小幅评分收益；覆盖单张、对子、三张、顺子及同类普通复合牌型，选择仍遵循各牌型的最小充分比较规则。
- 队友持权让牌、A/级牌/红桃级牌/大小王保护、炸弹保护、结构损伤成本和公开残局阻断均未放宽。
- 72/72 normal-vNext 固定牌例、类型、lint、格式与 5 seed 专项基准（测试主体 3.676 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-16 首轮低连对领牌修复

- 首轮领牌的完整、低点数自然三连对，可抵销仅由重叠连对产生的额外结构损伤；例如 `33 44 55 66` 与 `99 10 10 JJ` 并存时选择 `44 55 66`。
- 该抵销仅用于领牌评分；跟牌、尾局阻断、炸弹保护以及顺子、钢板等其他结构成本完全不变。
- 74/74 normal-vNext 固定牌例、类型、lint、格式与 5 seed 专项基准（测试主体 3.642 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-17 天然炸弹绝不拆分

- normal-vNext 将天然四张及以上同点数炸弹作为硬约束：机器人只会整副出炸，绝不以其中部分牌组成单张、对子、三张、顺子、连对、钢板或三带二。
- 该约束覆盖领牌、跟牌、直接收尾、尾局及强制阻断；不能整副出时可选择合法 `pass`。规则层合法动作集与真人操作不受影响。
- P7-10/P7-13 的所有拆炸例外均已撤销。75/75 normal-vNext 固定牌例、类型、lint、格式与 5 seed 专项基准（测试主体 2.697 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-18 领牌完整结构候选锚定

- 有界领牌候选池不再完全依赖规则层枚举顺序：完整自然顺子、三连对和钢板每类固定保留至多两个低点数候选，再填充普通候选。
- 因此 `56789` 即使处于规则动作列表后段，也能进入 normal-vNext 评分；不会再被单张候选挤出而拆成逐张领出。
- 保持 24 个常规深度评分候选的性能边界。78/78 normal-vNext 固定牌例、类型、lint、格式与 5 seed 专项基准（测试主体 3.187 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-19 领牌普通牌型候选锚定

- 有界领牌候选池会在配牌扩展候选填充前，固定保留自然单张、对子、三张、三带二各一个低点数代表；自然对子不会因红桃级牌的低对子投影排在前段而失去评分机会。
- 红桃级牌降配、控制牌保护、天然炸弹不拆、顺子/连对/钢板锚点及 24 个常规候选的性能上限继续生效。
- 79/79 normal-vNext 固定牌例、类型、lint、格式与 5 seed 专项基准（测试主体 3.191 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-20 回收牌权控制资源保留

- 策略将“回收牌权储备”纳入临门处理：公开可知两张大王均已打出时，小王是不可压制的后手控制牌；若非直接收尾且仍有其他合法领牌，不会仅为保持当前轮牌权而先耗小王。
- 强制跟牌不再机械选择最大压制，改用已有资源评分的最小充分合法动作，保留 A、级牌和王等后续同型接管资源；直接收尾、唯一合法动作和天然炸弹不拆分约束不受影响。
- normal-vNext 与桌面控制器回归 94/94、类型、lint、格式及 5 seed 专项基准（测试主体 2.945 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-21 低点数顺子路线卸载

- 领牌策略增加通用路线判断：对于不含控制牌的低点数自然顺子，若出顺子只会把一组自然三张降为仍可作为对子使用的结构，则优先一次卸载五张散牌；不再因同手牌的重叠顺子计数而退化为逐张领牌。
- 这不是指定点数或牌例特判。跟牌、最高点数大于 10 的顺子、红桃级牌/王、把三张拆到不足对子、以及所有天然炸弹保护均继续采用完整结构成本。
- normal-vNext 与桌面控制器回归 95/95、类型、lint、格式及 5 seed 专项基准（测试主体 2.785 秒）均通过；这不是 Preview 或 Production 发布验收。

## 2026-08-27：P7-22 完整自然对子跟牌

- 对手持权时，完整自然对子不再因其同时参与潜在顺子而被安全门槛整体拒绝；它会按同型最小充分比较加入跟牌选择，避免有对4、对J、对K仍无故过牌。
- 放宽严格限定于完整自然对子。单张、三带二、顺子等仍按既有结构损伤排序；王、级牌、红桃级牌和天然炸弹绝不拆分等资源保护不受影响。
- normal-vNext 与桌面控制器回归 96/96、类型、lint、格式及 5 seed 专项基准（测试主体 2.769 秒）均通过；这不是 Preview 或 Production 发布验收。
