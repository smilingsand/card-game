# P7 测试矩阵

## P7-00（accepted）

| 验收项               | 证据                                                                                                                                                                                | 结果                                                                                                                       |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 运行器可导入与可单测 | `frontend/scripts/normal-vnext-simulation-runner.test.ts`                                                                                                                           | 依赖注入下验证固定 seed 调度、报告写入和失败局拒绝；无需 CLI 或真实文件系统。                                              |
| CLI 薄封装           | `frontend/scripts/normal-vnext-simulation.ts`                                                                                                                                       | 仅解析/校验参数并调用 `runNormalVNextSimulation`；npm 命令使用 `vite-node --script` 转发业务参数。                         |
| 固定 seed 自动对局   | `npm.cmd run guandan:normal-vnext-simulation -- --profile=normal-vNext --seeds=0 --games-per-seed=1 --max-turns=1000 --output-dir=D:\\MyWorks\\card-game\\temp\\p7-00-runner-smoke` | 2026-08-26：完成 1/1，126 个动作，结算顺序 north/south/west/east；非法动作和最大回合失败均为 0。报告仅位于忽略的 `temp/`。 |
| 核心回归             | `cd frontend; npm.cmd run test:core`                                                                                                                                                | 2026-08-26 通过，含新增运行器 2 项测试。                                                                                   |
| 类型、lint、格式     | `cd frontend; npm.cmd run typecheck; npm.cmd run lint; npm.cmd run format:check`                                                                                                    | 2026-08-26 通过。                                                                                                          |

运行更大的固定 seed 集合属于 P7-05 收敛门槛；当前受控执行环境对多 seed 长运行会提前结束且不写报告，已保留为环境限制，不以单 seed 冒烟替代该门槛。

## P7-01（accepted）

| 验收项               | 证据                                                            | 结果                                                                                               |
| -------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 已公开牌面投影       | `public-action-projection.ts` 与 `strategy-observation.test.ts` | 只从 `action.applied` 的已打出 card ID 解析 `{id, suit, rank}`；不返回 `deckIndex`、牌堆或未出牌。 |
| 纯 observation 重建  | `createStrategyObservation` 固定牌例                            | 按 event sequence 排序；相同公共投影的逆序输入得到相同 observation。                               |
| 旧投影兼容           | 同一固定牌例                                                    | 没有 `publicActions` 的旧 BotView 返回空公开牌统计，不抛错或反查隐藏牌。                           |
| 四座泄露边界         | 同一固定牌例与 `BotView` 类型断言                               | 四座共享相同公开牌事实，只改变自己/队友/对手座位关系；输出无 `seed`、`opponentHands`。             |
| 连续赛局上下文与重放 | `table-session.test.ts`、`secure-seed.test.ts`                  | 上下文从 MatchSession 纯函数生成；存档恢复后重建相同上下文，secure seed 回放等价仍通过。           |

## P7-02（accepted）

| 验收项           | 证据                                         | 结果                                                                   |
| ---------------- | -------------------------------------------- | ---------------------------------------------------------------------- |
| 分项可复核       | `normal-vnext-bot.test.ts` 的 P7-02 固定牌例 | 总分严格等于点数、结构、控制、红桃级牌、附带牌成本减卸载/拦截收益。    |
| 机会成本         | 同上及既有 normal-vNext 牌例                 | 非必要红桃级牌成本更高；炸弹/三张/连续结构的拆分成本继续生效。         |
| 合法性与稳定排序 | 同上                                         | 非规则层合法动作不评分；同分动作按 JSON 固定 tie-break，重复决策确定。 |
| 核心回归         | `cd frontend; npm.cmd run test:core`         | P7-02 完成时执行，所有核心固定牌例、存档回放和运行器测试必须通过。     |

## P7-03（accepted）

| 验收项             | 证据                               | 结果                                                                             |
| ------------------ | ---------------------------------- | -------------------------------------------------------------------------------- |
| 协同与炸弹固定牌例 | `normal-vnext-bot.test.ts`         | 队友持权让牌、队友临门信号、公开临门对手阻断与公开控制暴露均只读 BotView。       |
| 炸弹经济           | `describeNormalVNextBombEconomics` | 主动炸弹仅在保队友、阻断公开临门对手或直接收尾时获得正向条件；其他炸弹保持后置。 |
| 回归               | normal-vNext 固定牌例              | 修复非炸弹评分的 `NaN` 排序回归后，54/54 通过。                                  |

## P7-04（accepted）

| 验收项           | 证据                           | 结果                                                                               |
| ---------------- | ------------------------------ | ---------------------------------------------------------------------------------- |
| 己方路线固定牌例 | `estimateNormalVNextSelfRoute` | 对每个规则层合法候选只分析己方剩余牌，输出死单、自然组、控制牌保留与固定估计手数。 |
| 确定性与边界     | 同一固定牌例                   | 同输入重复结果相同；无对手手牌、搜索、随机或墙钟预算。                             |

## P7-05（ready_for_acceptance）

| 验收项                    | 证据                                               | 结果                                                                                                                                                         |
| ------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| normal-vNext 专项性能门槛 | `normal-vnext-benchmark.test.ts`                   | 固定 seed `0, 1, 7, 42, 99` 只测生产选择器的首次决策；每个返回动作均属于规则层 `legalActions`。                                                              |
| 性能回归上限              | 同一测试                                           | 2026-08-26 用户本地复测通过：5 个 seed 的测试主体为 10.917 秒，最慢单次决策低于 5 秒。5 秒上限由 P7-00 的约 3.57 秒基线加回归余量确定，不把基线误述为 2 秒。 |
| 历史基准隔离              | `bot-benchmark.test.ts` 与 `core.vitest.config.ts` | 保留 normal/basic 百局历史基准作为独立慢测；`test:core` 改为执行 P7 专项基准，避免以不覆盖 normal-vNext 的长跑阻塞 P7 验收。                                 |
| 发布验收                  | Preview、Production 与 `release.md`                | 未执行；需在用户授权发布后按项目流程完成。                                                                                                                   |

## P7-06（accepted）

| 验收项                   | 证据                                                                                | 结果                                                                                                                                                              |
| ------------------------ | ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| observation 接入最终选牌 | `normal-vnext-bot.test.ts` 的 P7-06 公开控制牌牌例                                  | 公开已出的高控制牌可降低同类控制牌的保留成本，并改变最终合法候选；无对手手牌输入。                                                                                |
| 路线接入最终选牌         | 同一文件的 P7-06 路线牌例                                                           | 候选评分包含预计出手数、死单、自然组和控制牌保留；牌例验证路线更优的合法候选被选择。                                                                              |
| 炸弹经济接入最终选牌     | 同一文件的 P7-06 炸弹牌例                                                           | 仅当公开收益成立，且全部普通响应均更伤结构并留下更差路线时，炸弹可越过普通响应。                                                                                  |
| 有界性能                 | `normal-vnext-benchmark.test.ts`                                                    | 首次实现曾对全部领牌候选做路线排序，最慢决策达 203.833 秒；已修为 normal 基线锚点加 23 个额外候选的固定上限。2026-08-26：5 seed 专项基准通过，测试主体 4.452 秒。 |
| 类型与共享入口回归       | `npm.cmd run typecheck`；`table-controller.test.ts`；`strategy-observation.test.ts` | 2026-08-26：类型检查通过；桌面控制器 13/13、公开 observation 4/4、normal-vNext 58/58 通过。                                                                       |

## P7-07（accepted）

| 验收项     | 证据                                                      | 结果                                                                                   |
| ---------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 低价值跟牌 | `normal-vnext-bot.test.ts`                                | 普通中局可用低点数自然对子或三张拆出合法压制；原 777 过牌预期已按本任务替换。          |
| 高价值保护 | 同一固定牌例与既有 P6 牌例                                | A、级牌、大小王、红桃级牌、顺子、连对、钢板和炸弹仍不能通过此例外放宽。                |
| 类型与性能 | `npm.cmd run typecheck`；`normal-vnext-benchmark.test.ts` | 2026-08-26：类型检查通过，normal-vNext 60/60；5 seed 专项基准通过，测试主体 3.517 秒。 |

## P7-08（accepted）

| 验收项           | 证据                                                                                         | 结果                                                                                               |
| ---------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 头家走完后的清墩 | `frontend/src/visible-trick.test.ts`                                                         | 清墩状态立即投影为空，不会等待下一位动作或定时器而保留头家最后一手。                               |
| 新首出与跨局隔离 | 同一回归测试                                                                                 | 同局新首出只显示新墩动作；`roundNumber` 变化时不复用上一局的事件下标，其他座位的后续出牌继续显示。 |
| 前端回归         | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`npm.cmd run build` | 2026-08-26 通过；单人牌桌挂载/退出冒烟测试通过。                                                   |

## P7-09（accepted）

| 验收项                     | 证据                                                                                                                                  | 结果                                                                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 完整炸弹收官路线           | `normal-vnext-bot.test.ts` 的 P7-09 牌例                                                                                              | `77777 + 3333` 领出时，完整天然炸弹能严格减少预计己方出手数，优先于拆成 `77733` 的三带二；候选集在短手牌时额外保留至多 4 个天然炸弹。 |
| 配牌节约                   | 同一固定牌例                                                                                                                          | 红桃级牌作为非收尾候选的机会成本从 120 提高至 420；普通小牌存在时优先保留配牌。                                                       |
| 最小充分跟牌与首轮控制保护 | 同一固定牌例和既有 P6 牌例                                                                                                            | 开局跟小单仍选最小充分普通牌；普通结构可领出时不先消耗 `AAA222` 高控制钢板。                                                          |
| 类型、固定牌例与性能       | `npm.cmd run typecheck`、`normal-vnext-bot.test.ts`、`normal-vnext-benchmark.test.ts`、`npm.cmd run lint`、`npm.cmd run format:check` | 2026-08-26：64/64 normal-vNext 固定牌例通过；5 seed 专项基准通过，测试主体 2.720 秒。                                                 |

## P7-10（accepted）

| 验收项               | 证据                                                                                                      | 结果                                                                                                                       |
| -------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 炸弹默认保护         | `normal-vnext-bot.test.ts` 的 P7-10 牌例                                                                  | 即使非短手牌，领出也不为普通三带二拆开完整天然炸弹；候选集固定额外保留至多 4 个天然完整炸弹。                              |
| 显著收益例外         | 同一固定牌例                                                                                              | 拆炸后的预计后续出手数至少减少两手时，允许以顺子等复合牌取得显著己方路线收益。公开残局强制阻断与直接收尾仍由既有例外覆盖。 |

> P7-10 的拆炸例外为历史结果，已由 P7-17 撤销；运行时策略以 P7-17 为准。
| 类型、固定牌例与性能 | `npm.cmd run typecheck`、`normal-vnext-bot.test.ts`、`normal-vnext-benchmark.test.ts`、`npm.cmd run lint` | 2026-08-27：66/66 normal-vNext 固定牌例通过；5 seed 专项基准通过，测试主体 2.795 秒。                                      |

## P7-11（accepted）

| 验收项               | 证据                                                                                                      | 结果                                                                                              |
| -------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 红桃级牌降配         | `normal-vnext-bot.test.ts` 的 P7-11 牌例                                                                  | 两张红桃级牌按原生级牌对子可压过时，不再降配成普通对 9；每张降配为非级牌额外承担 700 的机会成本。 |
| 普通最小充分对优先   | 同一固定牌例                                                                                              | 有普通对 9、10、K、A 时，以对 9 压过对 8，不消耗将牌或配牌。                                      |
| 类型、固定牌例与性能 | `npm.cmd run typecheck`、`normal-vnext-bot.test.ts`、`normal-vnext-benchmark.test.ts`、`npm.cmd run lint` | 2026-08-27：68/68 normal-vNext 固定牌例通过；5 seed 专项基准通过。                                |

## P7-12（accepted）

| 验收项               | 证据                                                                                                     | 结果                                                                                               |
| -------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 配牌投影跟牌回归     | `normal-vnext-bot.test.ts` 的 P7-12 牌例                                                                 | 160 个三带二配牌投影与普通 `77733` 并存时，仍选择最小充分的普通三带二。                            |
| 跟牌深度评分有界     | `collectResponseAnalysisCandidates`                                                                      | 合法动作集不裁剪；超过 24 个跟牌候选时，仅以轻量、稳定的资源优先级预筛最多 24 个候选进入深度评分。 |
| 类型、固定牌例与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：类型、lint、格式均通过；70/70 专项测试通过，5 seed 性能测试主体为 2.809 秒。           |

## P7-13（accepted）

| 验收项               | 证据                                                                                                     | 结果                                                                                                    |
| -------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 谨慎尾局拆炸回归     | `normal-vnext-bot.test.ts` 的 P7-13 牌例                                                                 | 西家领牌、下家南家剩 5 张，`44 55 66` 不再拆开 6 张 4 炸、4 张 5 炸和 4 张 6 炸；选择一副完整天然炸弹。 |
| 例外边界             | 同一文件的既有 P7-10 与强制阻断牌例                                                                      | 直接收尾、己方路线至少缩短两手，以及下家 1–3 张的公开强制阻断继续生效。                                 |

> P7-13 的拆炸例外为历史结果，已由 P7-17 撤销；运行时策略以 P7-17 为准。
| 类型、固定牌例与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：类型、lint、格式均通过；71/71 专项测试通过，5 seed 性能测试主体为 3.635 秒。                |

## P7-14（accepted）

| 验收项               | 证据                                                                                                     | 结果                                                                                        |
| -------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 完整 KKK44 争牌回归  | `normal-vnext-bot.test.ts` 的 P7-14 牌例                                                                 | 四副天然炸弹并存时，完整自然 `KKK44` 对中等三带二会主动压制，不拆炸弹也不消耗 A、级牌或王。 |
| 最高控制边界         | 同一牌例及既有 `AAAKK`、`222AA` 牌例                                                                     | K 不再被误作最高控制牌；A、级牌、大小王和红桃级牌的保护仍生效。                             |
| 类型、固定牌例与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：类型、lint、格式均通过；72/72 专项测试通过，5 seed 性能测试主体为 4.258 秒。    |

## P7-15（accepted）

| 验收项                   | 证据                                                   | 结果                                                                                                  |
| ------------------------ | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |
| 全牌型普通跟牌偏好       | `normal-vnext-bot.test.ts` 的 P7-15 固定牌例           | 已覆盖对子、三张、顺子：对手持权时优先最小充分的完整普通压制，而非级牌；单张既有牌例继续覆盖。      |
| 专用排序与资源保护不回归 | 同一专项测试的三带二、公开控制与队友持权牌例           | 72/72 通过；三带二仍先比主三张，公开 A 暴露、将牌/王保护及队友让牌仍生效。                           |
| 类型、格式、lint 与性能  | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`normal-vnext-benchmark.test.ts` | 2026-08-27：均通过；72/72 专项固定牌例通过，5 seed 基准主体 3.676 秒，最慢单次决策低于 5 秒。 |

## P7-16（accepted）

| 验收项                 | 证据                                                                                                      | 结果                                                                                                                     |
| ---------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 首轮低连对优先         | `normal-vnext-bot.test.ts` 的 P7-16 重叠连对牌例                                                         | `33 44 55 66` 与 `99 10 10 JJ` 并存时，首轮选择完整低连对 `44 55 66`，不再因潜在 `33 44 55` 额外损伤而改出高连对。 |
| 适用边界               | 同一文件的 P7-16 跟牌牌例及 P7-10/P7-13 炸弹回归                                                        | 抵销仅在领牌生效；跟牌仍承担完整结构损伤，尾局阻断、天然炸弹保护和顺子/钢板等其他结构成本未放宽。                     |
| 类型、格式、lint 与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、`normal-vnext-benchmark.test.ts` | 2026-08-27：均通过；74/74 专项固定牌例通过，5 seed 基准主体 3.642 秒，最慢单次决策低于 5 秒。                        |

## P7-17（accepted）

| 验收项                     | 证据                                                                                                      | 结果                                                                                                             |
| -------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 全策略硬约束               | `normal-vnext-bot.ts` 的候选入口过滤                                                                      | 天然四张及以上同点数炸弹只可整副出；拆分动作不会进入领牌、跟牌、尾局阻断、强制阻断或回退候选。                 |
| 撤销旧拆炸例外             | `normal-vnext-bot.test.ts` 的 P7-17 领牌与强制阻断固定牌例                                                | 即使拆炸能缩短路线，或下家仅剩一张需强制阻断，也不会拆；无法整副出时选择合法 `pass`。                           |
| 类型、格式、lint 与性能    | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：均通过；75/75 专项固定牌例、5 seed 基准通过，测试主体 2.697 秒，最慢单次决策低于 5 秒。            |

## P7-18（accepted）

| 验收项                   | 证据                                                                                                      | 结果                                                                                                                         |
| ------------------------ | --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 后段完整结构候选保留     | `normal-vnext-bot.test.ts` 的 P7-18 顺子、连对、钢板固定牌例                                            | 即使三类完整自然结构排在 23 个普通候选之后，仍进入领牌评分并优先于拆结构的单张。                                           |
| 候选池有界               | `collectLeadAnalysisCandidates`                                                                           | 保持最多 24 个常规深度评分候选；在填充普通候选前，对顺子、连对、钢板每类固定保留至多 2 个完整自然结构锚点。                |
| 类型、格式、lint 与性能  | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：均通过；78/78 专项固定牌例、5 seed 基准通过，测试主体 3.187 秒，最慢单次决策低于 5 秒。                       |

## P7-19（accepted）

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| 自然普通牌型候选保留 | `normal-vnext-bot.test.ts` 的 P7-19 固定牌例 | 即使前段存在 23 个红桃级牌降配为低对子投影，后段自然对子仍进入领牌评分并被选中。 |
| 候选池有界 | `collectLeadAnalysisCandidates` | 在既有顺子、连对、钢板锚点之外，对单张、对子、三张、三带二每类至多保留一个完整自然候选；常规深度评分仍最多 24 个候选。 |
| 类型、格式、lint 与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：79/79 专项固定牌例通过；类型、lint、格式通过；5 seed 基准测试主体 3.191 秒，最慢单次决策低于 5 秒。 |

## P7-20（accepted）

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| 临门领牌保留回收控制 | `normal-vnext-bot.test.ts` 的 P7-20 固定牌例 | 大王两张均已公开打出时，小王为不可压制后手；下家两张而己方尚有 A、10 时，领 A 而非无意义先耗小王；仅剩小王可直接收尾时仍可出。 |
| 强制跟牌最小充分 | `normal-vnext-bot.test.ts` P7-20 与 `table-controller.test.ts` 回归 | 临门强制跟牌改用最小充分合法牌；小王足够时不烧大王，普通 6 足够时不烧 A。 |
| 类型、格式、lint 与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 专项测试与性能门槛 | 2026-08-27：normal-vNext 与桌面控制器回归 94/94 通过；类型、lint、格式通过；5 seed 基准测试主体 2.945 秒，最慢单次决策低于 5 秒。 |

## P7-21（accepted）

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| 低点数顺子路线卸载 | `normal-vnext-bot.test.ts` 的 P7-21 通用结构回归 | 领牌时，完整低点数自然顺子若仅将一组自然三张降为仍保留的对子，会进入优先卸载路线，不再被逐张低牌压过。 |
| 适用边界 | `leadingLowStraightUnloadBenefit` | 仅适用于领牌、五张自然顺子、最高点数不超过 10、无王/红桃级牌，且三张必须保留为对子；跟牌、控制牌、高点数顺子与天然炸弹保护均不放宽。 |
| 类型、格式、lint 与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 与桌面控制器回归、性能门槛 | 2026-08-27：95/95 回归通过；类型、lint、格式通过；5 seed 基准测试主体 2.785 秒，最慢单次决策低于 5 秒。 |

## P7-22（accepted）

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| 完整自然对子优先跟牌 | `normal-vnext-bot.test.ts` 的 P7-22 通用结构回归 | 对手持权时，低对子即使同时参与潜在顺子，仍作为完整自然普通对子进入安全候选；在对4、对J、对K并存时选择最小充分的对4，不再 pass。 |
| 适用边界 | `isCompleteNaturalPairFollowResponse` | 仅完整自然对子；单张、三带二、顺子与其他复合牌型继续使用既有结构排序；王、级牌、红桃级牌和天然炸弹保护不放宽。 |
| 类型、格式、lint 与性能 | `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run format:check`、normal-vNext 与桌面控制器回归、性能门槛 | 2026-08-27：96/96 回归通过；类型、lint、格式通过；5 seed 基准测试主体 2.769 秒，最慢单次决策低于 5 秒。 |

## P7-23（accepted）

| 验收项 | 证据 | 预期 / 当前结果 |
| --- | --- | --- |
| 公开残局多炸弹夺权 | `normal-vnext-bot.test.ts` 的 P7-23 固定牌例 | 任一对手剩余 6 张、己方两副完整天然炸弹时，机器人整副炸弹压制并夺回牌权；只使用 `remainingCardCounts`，不读取对手手牌。84/84 normal-vNext 固定牌例通过。 |
| 资源与规则边界 | `isNaturalWholeBomb`、`splitsNaturalBomb`、P7-17 回归 | 仅完整天然炸弹可触发；不拆炸，队友临门让牌、直接收尾和既有强制阻断优先级不变。 |
| 格式、类型、专项与性能 | `npm.cmd run format:check`、typecheck、lint、Vitest 专项及 5 seed 基准 | 2026-08-27：格式、类型和 lint 通过；5 seed 基准测试主体 2.900 秒，最慢单次决策低于 5 秒。 |
