# P3 测试矩阵

## P3-11 测试矩阵（ready_for_acceptance）

执行边界：以 Miniflare + SQLite Durable Object 为主门禁；仅在主门禁全绿后执行本地 Vite → Wrangler 浏览器联调。禁止 Preview、Production、Cloudflare 登录或部署。

| 类别               | 注入/次数                                                                                                       | 必过断言                                                                                                                                                                                                              |
| ------------------ | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 人数与机器人补位   | 1+3、2+2、3+1、4 真人，各 5 局                                                                                  | 每席控制权唯一；空席 normal-vNext 仅经 Room → Authority 内部命令行动；全局无非法/重复动作或事件序号异常，并可结算。                                                                                                   |
| 断线、超时与恢复   | 每逻辑座断线重连、回合超时托管、托管后恢复各 3 次                                                               | 只在动作边界交接；原 subject 回原 seat；机器人行动完成后交还；个人投影不泄露 seed、他人手牌或隐藏评估。                                                                                                               |
| 生命周期           | Worker/DO 重启 5 次；过期 3 次                                                                                  | 重启后投影、序号、回放一致；过期后拒绝读取和动作；恢复审计可用于定位 room/event，且不向客户端暴露备份或 seed。                                                                                                        |
| 协议与有序性       | 兼容/拒绝各 3 次；ACK 重复、乱序、缺口各 5 次                                                                   | 兼容版本收敛；不兼容版本明确拒绝；重复命令返回原 ACK，乱序不污染状态，缺口仅重放连续个人投影。                                                                                                                        |
| 可记录本地混沌注入 | 8 类：正常 close、30 秒心跳失联、30 秒回合超时、重连边界、DO 重启、状态损坏后恢复、事件缺口恢复、协议版本不兼容 | 每项记录受控 `roomId`、服务端审计可关联的 event/步骤和首失败复现信息；测试日志、客户端消息与断言输出均不得含 seed、令牌、Cookie、他人手牌或隐藏评估。失败保留完整受控 replay 与首失败 seed 于测试临时目录，禁止提交。 |
| 本地浏览器联调     | 4 个独立上下文同房                                                                                              | 每个真人自己位于 bottom；完成至少一回合；验证断线重连缺口、超时托管和动作边界恢复。                                                                                                                                   |

对任一修复，受影响的 P3-11 用例须连续运行 3 次；主 agent 综合验收前执行后端类型、格式和既有 P3-03/05/06/08/09/10 回归。

自动化证据（2026-07-26）：`cd backend; npm.cmd run test:p3-11` 通过 5/5（约 282 秒）。覆盖 1–3 真人空座机器人首调度各 10 局、4 真人 5 局、四逻辑座位断线/超时/托管/恢复各 3 次、DO 重启 5 次、兼容与拒绝协议各 3 次、ACK 重复/乱序/缺口各 5 次、固定 seed 五局完整结算和房间过期 3 次。测试只经 Room → Authority 路径提交动作，所有个人投影均扫描 seed、他人手牌和隐藏评估泄露。浏览器自动化不作为本任务门槛；请按 [P3-11 本地浏览器人工验收](P3-11-local-browser-acceptance.md) 取得人工证据，之前不得标记 `accepted`。

## P3-10 测试矩阵（accepted）

| 验收项               | 证据                                                                                                                                                                                                                           | 当前结果                                                                                                                                                |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 受控备份、恢复与幂等 | `cd backend; npm.cmd run test:p3-10`                                                                                                                                                                                           | Miniflare SQLite DO：生成带 SHA-256 校验摘要的受控备份；篡改快照后恢复最近有效备份，事件重放一致，已持久化命令返回原 ACK。                              |
| 失败证据与脱敏审计   | 同上                                                                                                                                                                                                                           | 校验摘要不匹配的备份返回稳定 422、保持权威状态不变，并写入只含截断 room 标识、序号和错误类别的结构化审计；日志不含 token、Cookie、显示名称或明文 seed。 |
| 本地恢复演练         | 同上（首个 P3-10 用例）                                                                                                                                                                                                        | 已实际执行“快照损坏 → 拒绝读状态 → 受控恢复 → 重放与命令幂等核验”；RPO 为 0 个已确认事件，测试耗时约 1 秒，低于 RTO≤15 分钟目标。                       |
| P3 回归、类型与格式  | `cd backend; npm.cmd run test:p3-03; npm.cmd run test:p3-05; npm.cmd run test:p3-06; npm.cmd run test:p3-08; npm.cmd run test:p3-09; npm.cmd run typecheck; node ../frontend/node_modules/prettier/bin/prettier.cjs --check .` | P3-03/04 14 项、P3-05 3 项、P3-06 4 项、P3-08 4 项、P3-09 3 项、P3-10 4 项、TypeScript 与 Prettier 全部通过。                                           |

生产 R2 对象存储绑定和真实告警接收端仍属于 P3-12 的 Cloudflare 部署授权范围；本任务未登录、部署或创建云资源。本地演练目录固定为 `backend/temp/backups/` 且已忽略。

## P3-09 测试矩阵（accepted）

| 验收项                           | 证据                                                                                                                                                                                                   | 当前结果                                                                                                                                                                                               |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 四个逻辑座位的个人投影与权限矩阵 | `cd backend; node ../frontend/node_modules/vitest/vitest.mjs run test/p3-09-security.local.test.mjs --config ../frontend/vite.config.ts --configLoader runner`                                         | 四席各仅获得自己的 27 张实体手牌；其他三席全部实体牌 ID、seed、加密 seed、隐藏评估均不在投影中。伪造 `actor` 与陌生会话读取房间均被拒绝。                                                              |
| 会话固定与重放                   | 同上                                                                                                                                                                                                   | 令牌轮换后旧 Cookie 和混合 `subjectId/token` Cookie 均返回 401；当前令牌仍有效。日志只包含脱敏字段。                                                                                                   |
| 模糊输入与压力滥用               | 同上                                                                                                                                                                                                   | 所有 P3 HTTP POST 统一复用既有 4 KiB JSON 边界；超限房间创建返回 413。12 个并发会话请求在固定每分钟 3 次的本地配置下恰有 3 次成功、9 次 429 且均含 `Retry-After`；拒绝/限流日志不含令牌、seed 或手牌。 |
| 回归、类型与格式                 | `cd backend; npm.cmd run typecheck; npm.cmd run test:p3-03; npm.cmd run test:p3-05; npm.cmd run test:p3-06; npm.cmd run test:p3-08; node ../frontend/node_modules/prettier/bin/prettier.cjs --check .` | TypeScript、P3-03/04 14 项、P3-05 3 项、P3-06 4 项、P3-08 4 项和 Prettier 全部通过。                                                                                                                   |

## P3-08 测试矩阵（accepted）

| 验收项                                 | 证据                                                                                                        | 当前结果                                                                                                                                |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 正常关闭、当前回合 10 秒宽限与安全接管 | `cd backend; npm.cmd run test:p3-08`                                                                        | WebSocket `1000` 立即记为断线；宽限届满后仅通过 Room → Authority 内部命令执行 `normal-vNext`，个人投影不泄露 seed、他人手牌或隐藏评估。 |
| 心跳、异常失联与 30 秒回合超时         | 同上                                                                                                        | 客户端每 10 秒心跳；Room 持久化最后活动时间并以 30 秒判定失联，Authority 持久化回合起点并在超时边界启用托管。                           |
| 回归与大厅保留                         | `cd backend; npm.cmd run test:p3-03; npm.cmd run test:p3-05; npm.cmd run test:p3-06; npm.cmd run typecheck` | P3-03/04 14 项、P3-05 3 项、P3-06 4 项和 TypeScript 均通过；大厅房主断线 60 秒转让给最早在线真人，无其他真人时 5 分钟关闭。             |
| 主 agent 综合验收                      | P3-08 全量回归、前后端 typecheck、前端 lint/build、`git diff --check`                                       | 2026-07-26：全部通过；生产构建首次仅受沙箱临时文件写入限制影响，获授权后在相同工作区重跑通过。                                          |

## P3-07 测试矩阵（accepted）

| 验收项                                          | 证据                                                                                                 | 当前结果                                                                                                                        |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 多人大厅、预设/自定义名称、房间与机器人空座展示 | `cd frontend; npm.cmd run test:run -- --configLoader runner src/multiplayer/MultiplayerApp.test.tsx` | 组件测试 4/4 通过：创建请求只提交名称和逻辑座位；支持自定义名称；四席视觉投影保持本人在 bottom；出牌意图不携带可信 actor/seat。 |
| 前端静态检查与单机隔离                          | `cd frontend; npm.cmd run typecheck; npm.cmd run lint; npm.cmd run build`                            | 通过；单机 IndexedDB 仅在 `SoloApp` 子树挂载，多人模块只保存个人投影、房间投影和连接序号。                                      |
| 本地 Vite → Wrangler 冒烟                       | 运行本地 Worker 与 Vite 后访问 `/`、`/v1/session`                                                    | 通过：Vite 主页 200，`/v1/session` 经代理返回 201 与重连 cookie；未登录、未部署或创建云资源。                                   |

## P3-06 测试矩阵（accepted）

| 验收项                                   | 证据                                                                                | 当前结果                                                                                      |
| ---------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| WebSocket 协商、ACK、乱序/重复与缺口恢复 | `cd backend; npm.cmd run test:p3-06`                                                | 本地 Miniflare：`p3-ws-v1` 协商、命令 ACK 幂等、延迟旧序号冲突、丢包后的 `resync` 重放均通过  |
| 重启恢复、未授权和协议拒绝               | 同上                                                                                | SQLite DO 冷启动后从序号 0 重放；陌生会话为 403；不兼容版本返回 `protocol.unsupported` 后关闭 |
| 后端回归与类型                           | `cd backend; npm.cmd run typecheck; npm.cmd run test:p3-03; npm.cmd run test:p3-05` | 类型检查通过；P3-03/04 共 14 项、P3-05 共 3 项回归通过                                        |

ADR-0029 已冻结真人 `subject → roomId → logical seat` 控制链；Authority 忽略客户端伪造 actor，并按绑定座位与权威回合执行动作。四个视角的显示旋转只发生在个人投影/UI 层，不改变逻辑行动顺序。

## P3-05 测试矩阵（accepted）

| 验收项                                   | 证据                                 | 当前结果            |
| ---------------------------------------- | ------------------------------------ | ------------------- |
| 房间创建、邀请制受控加入与名字唯一性     | `cd backend; npm.cmd run test:p3-05` | 本地 Miniflare 通过 |
| 1–4 真人、满房、准备/开始与 bot 空座     | 同上                                 | 本地 Miniflare 通过 |
| 座位请求须由房主批准，且不泄露 seed/手牌 | 同上                                 | 本地 Miniflare 通过 |

| 验收项               | 证据                                                                      | 结果                                    |
| -------------------- | ------------------------------------------------------------------------- | --------------------------------------- |
| P3-02 浏览器固定回放 | `cd frontend; npm.cmd run test:run -- core-cross-runtime.browser.test.ts` | 固定 seed、初始状态和动作流的浏览器回放 |
| P3-02 Node 固定回放  | `cd frontend; npm.cmd run test:core --workspace=card-game-backend`        | `backend/` 仅以包名消费同一核心         |

P3-02 主验收（2026-07-23）：`node frontend/node_modules/typescript/bin/tsc -p packages/guandan-core/tsconfig.json --noEmit --pretty false`、前端两份 tsconfig、ESLint、Prettier、Vite production build 均通过；浏览器固定回放与 App 测试共 20 项通过；Node 后端固定回放 1 项通过；核心 table-session 与 BotView 牌例 14 项通过。共享包只使用 ES2022 类型，不依赖 DOM、React、Vite、Cloudflare 或 Node 专属 API。

## P3-03 测试矩阵（accepted）

| 验收项                               | 证据                                                                                                                          | 当前结果                                                                                          |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| Worker 健康检查与 SQLite DO 本地链路 | `cd backend; npm.cmd run test:p3-03`                                                                                          | Miniflare SQLite DO：`/health`、匿名会话、令牌轮换并使旧令牌失效、模糊输入拒绝、限流，共 6 项通过 |
| 配置、鉴权与日志脱敏                 | `cd backend; npm.cmd run typecheck`；同上安全单测                                                                             | 配置 schema、跨身份访问拒绝、Cookie 安全属性与 token/seed/手牌字段脱敏均通过                      |
| 既有共享核心边界与固定回放           | `cd backend; npm.cmd run test:core`                                                                                           | 2 项通过；后端仍只通过 `@card-game/guandan-core` 消费共享核心                                     |
| 格式、CLI 与差异检查                 | `cd backend; node ../frontend/node_modules/prettier/bin/prettier.cjs --check .`；`npx wrangler --version`；`git diff --check` | Prettier 通过；Wrangler `4.113.0`；差异检查通过                                                   |

本任务未执行 `wrangler login`、部署或任何云资源操作；`wrangler.jsonc` 仅定义本地 Worker/SQLite Durable Object 配置。Wrangler、Miniflare 与官方 Workers 类型为锁定的本地开发依赖；黑盒测试以 `new_sqlite_classes` 对应的 SQLite DO 配置运行。P3-12 前仍不得进行公网部署。

# P3-04 测试矩阵（accepted）

| 验收项                       | 证据                                                                                                    | 当前结果                                                                                                                                    |
| ---------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 256 位 seed 与旧 number 兼容 | `cd frontend; npm.cmd run test:core -- --run ../packages/guandan-core/src/platform/secure-seed.test.ts` | 同一 64 位十六进制 seed 的洗牌、事件流、序列化与恢复一致；尾部 word 会影响洗牌；旧 number seed 与 `Math.random()` 禁用回归受测。            |
| Worker/Node 权威回归         | `cd backend; npm.cmd run typecheck; npm.cmd run test:p3-03`                                             | 本地 Miniflare SQLite DO：个人投影、动作 ACK 幂等、并发串行化、TTL、未授权回放、冷启动恢复均通过。                                          |
| seed 生命周期与快照完整性    | 同上 P3-04 用例                                                                                         | 两次新比赛具有不同 `gameId` 和受控内部 seed 指纹；冷启动保留同局 seed；重新开赛清除旧命令/事件/快照；篡改快照后只返回安全错误且不泄露状态。 |

说明：完整 `npm.cmd run test:core` 在当前单次 64 秒执行上限内未完成且未输出失败；P3-04 相关的 `secure-seed.test.ts`（4 项）与 `table-session.test.ts`（12 项，含显式 secure 下一局）已单独通过。

# P3-01 测试矩阵

| 验收项                         | 证据                                            | 结果                                                                                             |
| ------------------------------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 名称池、规范化、房内唯一       | `node --test backend/poc/name-policy.test.mjs`  | 2026-07-23：3/3 通过                                                                             |
| 四客户端连接、断连、冷启动恢复 | `cd backend/poc; npm.cmd test`                  | 2026-07-23：Miniflare 四客户端 POC 通过；见 `backend/poc/README.md`                              |
| 成本、连接上限、时延测量       | `cd backend/poc; npm.cmd run measure`；ADR-0025 | 2026-07-23：本地四席/重连批次已测；当前与早期公网 MVP 均固定 Workers Free，线上配额在 P3-12 实测 |
| 本地与公网授权边界             | ADR-0025；P3 README                             | 本地为 Vite → Wrangler → SQLite DO；P3-12 前禁止部署，届时等待用户完成 Free 账号与本机登录授权   |
| 协议版本、CSPRNG、回放权限     | ADR-0026、ADR-0028                              | 设计已冻结；P3-04/P3-06 实现回归                                                                 |

本机 POC 不创建云资源。禁止提交密码、API Key、token、本地登录凭据或 `.env`。Cloudflare 仅承载后端，不替代 Vercel 前端/PWA。
