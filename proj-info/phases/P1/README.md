# P1：单人本地掼蛋 MVP

## 当前任务

`P1-01`：前端工程初始化（已验收）。

## 范围约束

- 用户已明确授权先执行纯工程初始化；这不代表 P0 规则冻结完成。
- 本阶段当前不得实现掼蛋规则、牌桌 UI、机器人或联网。
- P1 后续任务仍须等待 `docs/resolved-rules.md` 的待确认项关闭。

## 完成定义

P1-01 的 package、严格 TypeScript、Vitest、ESLint、Prettier、最小 CI 均可运行，并且构建产物及 npm 缓存不进入根目录或 Git。
