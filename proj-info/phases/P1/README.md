# P1：单人本地掼蛋 MVP

## 当前任务

`P1-02`：公共平台类型（待开始）。

## 范围约束

- P0 规则已于 2026-07-13 冻结；实现以 `docs/resolved-rules.md` 和 ADR-0001 为准。
- 仅按活动阶段计划中依赖已满足的唯一任务推进；不得顺带实现未来任务。

## 完成定义

P1-01 的 package、严格 TypeScript、Vitest、ESLint、Prettier、最小 CI 均可运行，并且构建产物及 npm 缓存不进入根目录或 Git。
