# P2.7 - normal-vNext 策略稳定化交接

## 当前产品状态

- 当前分支：`main`。
- 当前本地版本：P2.7 - normal-vNext 策略稳定化；npm 包版本 `2.7.0`。
- 唯一产品机器人和提示策略：normal-vNext。
- normal-v1 仅保留为离线历史对照；P2.5 expert-24 已撤销，恢复必须先建 ADR。
- 规则版本仍为 `guandan-v5`，存档 schema 仍为 4；本次不改规则引擎或存档。

## 本轮已完成

1. 下家 1～6 张尾局威胁；下家 1～3 张的强制阻断与结构例外。
2. 策略层合法动作兜底：跟牌无压制必定 pass；领牌必定从 legalActions 选择 play。
3. 自然中小对子、三张和三带二的开中局争牌；高价值三带二普通局面 pass。
4. A、普通级牌、红桃级牌、小王和大王的递增控制资源保护；红桃级牌保留逢人配机会成本。
5. 明牌时按当前墩最后动作的事件顺序设置座位出牌层级，后出牌覆盖先出牌。

## 验证

- `npm.cmd run typecheck`：通过。
- `npm.cmd run lint`：通过。
- normal-vNext、策略指标、table-controller 与 App 的本轮针对性回归均通过；详细矩阵见 `proj-info/phases/P2/test-matrix.md`。

## 新 session 读取顺序

1. `AGENTS.md`
2. `README.md`
3. `proj-info/phases/README.md`
4. `proj-info/phases/P1-P3-execution-plan.md`
5. `docs/resolved-rules.md`
6. `docs/architecture.md`
7. `docs/基础机器人策略说明_V2.md`（涉及机器人时）
8. 本交接文档与 `git status --short`

## 后续

- P2.7 尚未有新的 Vercel Preview/Production 验收，不得将 P2-06 的线上证据冒充为 P2.7 发布证据。
- 若继续调整 normal-vNext，先写固定 BotView 牌例，再改策略；只允许消费 BotView 和规则层 legalActions。
- 下一可启动阶段任务仍为 P3-01。
