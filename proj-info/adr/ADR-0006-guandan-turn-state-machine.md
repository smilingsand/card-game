# ADR-0006：掼蛋回合状态机与动作事件

- 状态：已接受
- 日期：2026-07-13

## 决策

状态机仅接受统一的 `play` 与 `pass` 动作，先经同一 `validateAction` 再由 `applyAction` 产生新状态。状态包含当前领出者、当前最高解释、待响应座位、连续过牌数、完成顺序和剩余手牌数；已完成玩家永远从待响应集合移除。其余可行动玩家均过牌时清轮：最后成功出牌者领出；若其末手出完，则对家接风。无法行动时返回受控错误，不进入循环。

## 后果

P1-08 不处理进贡、升级或开局发牌；这些由 P1-09 接入。
